switch (title.orient) {
  case "left":
    // Code to execute if title.orient is "left"
    break;
  case "right":
    // Code to execute if title.orient is "right"
    break;
  case "center":
    // Code to execute if title.orient is "center"
    break;
  default:
    // Code to execute if title.orient doesn't match any of the cases
}
